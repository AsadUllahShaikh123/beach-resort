import { Container } from '@mui/material';
import React from 'react';
import Heading from '../Heading';

const Search = () => {
  const heading = 'Search Rooms '

  return (
    <>
      <div style={{height:'90vh'}}>
         <Heading heading={heading} />
         <Container>
           
         </Container>
      </div>
    </>
  )
  
};

export default Search;
